const { Component } = Shopware;

/**
 * @deprecated tag:v2.0.0 - Component will be removed - use swag-paypal-locale-field instead
 */
Component.extend('sw-paypal-locale-field', 'swag-paypal-locale-field', {
});
