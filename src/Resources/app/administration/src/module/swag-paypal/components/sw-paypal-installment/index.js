const { Component } = Shopware;

/**
 * @deprecated tag:v2.0.0 - Component will be removed - use swag-paypal-installment instead
 */
Component.extend('sw-paypal-installment', 'swag-paypal-installment', {
});
