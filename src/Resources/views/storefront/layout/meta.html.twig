{% sw_extends '@Storefront/layout/meta.html.twig' %}

{% block layout_head_stylesheet %}
    {{ parent() }}

    {% if isHMRMode %}
        {# Served by the javascript file #}
    {% else %}
        <link rel="stylesheet" href="{{ asset('bundles/swagpaypal/storefront/css/swag-pay-pal.css') }}">
    {% endif %}
{% endblock %}
